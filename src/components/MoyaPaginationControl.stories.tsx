import { MoyaPaginationControl } from "./MoyaPaginationControl";
import { Meta, StoryObj } from '@storybook/react';

const meta: Meta<typeof MoyaPaginationControl> = {
    component: MoyaPaginationControl,
}

export default meta;
type Story = StoryObj<typeof MoyaPaginationControl>;

export const Primary: Story = {
}
